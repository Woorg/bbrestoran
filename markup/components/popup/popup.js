(function () {

    $(function () {
        // contacts popup

        const $contactsPopup = $('.is_popup');

        $contactsPopup.magnificPopup({
            type: 'inline',
            // fixedContentPos: false
            mainClass: 'contacts-popup'

        });

    });

})();
